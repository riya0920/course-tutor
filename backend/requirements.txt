# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# LLM
anthropic>=0.40.0

# Retrieval (all optional at runtime — the app degrades gracefully without them)
PyMuPDF==1.25.1
chromadb==0.5.23
sentence-transformers==3.3.1

# Evals / finetuning (optional)
openai>=1.55.0
